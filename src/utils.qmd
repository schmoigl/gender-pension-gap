
```{ojs}
colors = ["#be0021", "#ffa500", "#0021be", "#be9a00", "#800080", "#ffd700", "#808080"]
function makeWidth(inputWidth) {
  const outputWidth = 
    inputWidth > 2000 ? inputWidth * 0.35 :
    inputWidth > 1400 ? inputWidth * 0.5 :
    inputWidth * 0.66
  return outputWidth
}
```